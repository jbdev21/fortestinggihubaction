name: Laravel

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Deploy to Production
      uses: appleboy/ssh-action@master
      with:
        host: 192.185.52.10
        username: ugohoday
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        port: 22
        use_insecure_cipher: true
        script: |
          cd applications/ForSample
          git pull origin main
        
